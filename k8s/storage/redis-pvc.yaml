# =============================================================================
# STANDALONE REDIS PVC - COMMENTED OUT
# =============================================================================
# This PVC is commented out because we are using the existing
# Redis Enterprise cluster (mcp-database) in the 'redis' namespace.
# 
# If using standalone Redis, update storageClassName to 'ceph-rbd-fast'
# to match existing infrastructure storage classes.
# =============================================================================

# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: redis-pvc
#   namespace: search-infrastructure
# spec:
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 10Gi
#   storageClassName: ceph-rbd-fast
